<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<link rel="stylesheet" href="/sstyle.css">
<body>
<!-- CSR And SSR 
client server rendering   popular (dynamic)(data change hota hain)

server sender rendring  jitni bhi client side ki server se render page banke pehle se taiyar  
response time fast (data change nahi hoga)
jab bhi pages server se render karyege to server side rendering 
this is what we are doing 
-->
<h1>Show user</h1>
<div class="user-card">
<% for(let user of Users){%>
   <h1 ><%= user.email%></h1>
   <h2 class="input"><%= user.password%></h2>
   <h3 class="input"><%= user.contact%></h3>
   <a class="buttons" id=one href="/u1/user/update/<%=user.id%>">Update</a>
   <form class="buttons" id=two action="/u1/user/delete/<%= user.id%>?_method=DELETE"method="POST">
    <button type="submit">Delete</button>
   </form>
<% } %>
</div>
  
</body>
</html>